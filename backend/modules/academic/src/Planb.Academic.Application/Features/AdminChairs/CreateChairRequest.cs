namespace Planb.Academic.Application.Features.AdminChairs;

/// <summary>Body del alta. La materia va en la ruta, que es como se entra a la pantalla.</summary>
public sealed record CreateChairRequest(string Name);
