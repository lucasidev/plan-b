import { ComingSoon } from '@/components/ui';

export default function ProfessorsPage() {
  return (
    <ComingSoon
      section="Docentes"
      futureUs="US-003"
      description="Catálogo de docentes con sus reseñas, ratings y materias que dictan. Cada docente aparece anonimizado en las reseñas pero su nombre figura como docente reseñado (ADR-0009)."
    />
  );
}
