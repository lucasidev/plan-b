import { ComingSoon } from '@/components/ui';

export default function ProfilePage() {
  return (
    <ComingSoon
      section="Mi perfil"
      futureUs="US-012"
      description="Tu perfil de alumno con carrera, plan, año de ingreso, progreso. Aterriza cuando se cree el StudentProfile (US-012)."
    />
  );
}
