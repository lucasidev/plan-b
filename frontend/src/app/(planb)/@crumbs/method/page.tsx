import { GenericCrumbs } from '../_lib/generic-crumbs';

/** Migas de /method: toda ruta de (planb) tiene su página en el slot (ver `_lib/generic-crumbs.tsx`). */
export default function MethodCrumbs() {
  return <GenericCrumbs />;
}
