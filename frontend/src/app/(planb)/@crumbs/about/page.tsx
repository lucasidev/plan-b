import { GenericCrumbs } from '../_lib/generic-crumbs';

/** Migas de /about: toda ruta de (planb) tiene su página en el slot (ver `_lib/generic-crumbs.tsx`). */
export default function AboutCrumbs() {
  return <GenericCrumbs />;
}
