import { GenericCrumbs } from '../../_lib/generic-crumbs';

/** Migas de /teachers/[id]: toda ruta de (planb) tiene su página en el slot (ver `_lib/generic-crumbs.tsx`). */
export default function TeacherCrumbs() {
  return <GenericCrumbs />;
}
