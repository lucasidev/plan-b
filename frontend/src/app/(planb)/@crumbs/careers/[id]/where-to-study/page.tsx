import { GenericCrumbs } from '../../../_lib/generic-crumbs';

/** Migas de /careers/[id]/where-to-study: toda ruta de (planb) tiene su página en el slot (ver `_lib/generic-crumbs.tsx`). */
export default function WhereToStudyCrumbs() {
  return <GenericCrumbs />;
}
