import { GenericCrumbs } from '../../../_lib/generic-crumbs';

/** Migas de /careers/[id]/plans: toda ruta de (planb) tiene su página en el slot (ver `_lib/generic-crumbs.tsx`). */
export default function CareerPlansCrumbs() {
  return <GenericCrumbs />;
}
