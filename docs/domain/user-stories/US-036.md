# US-036: Auth rebuild — 4 rutas separadas

**Status**: Backlog
**Sprint**: candidato a S2
**Epic**: [EPIC-02 — Identidad y autenticación](../epics/EPIC-02.md)
**Priority**: Medium
**Effort**: M
**ADR refs**: [ADR-0041](../../decisions/0041-rediseno-ux-post-claude-design.md), [ADR-0019](../../decisions/0019-single-nextjs-app-con-route-groups.md), [ADR-0023](../../decisions/0023-auth-flow-jwt-cookie-layout-guards.md)

## Como member visitante, quiero rutas dedicadas para cada flow de auth para no mezclar verbos distintos en una sola pantalla con tabs

El AuthView que aterrizó en S1 (`/auth` con tabs Sign-up / Sign-in) economizó código pero mezcló dos verbos distintos: registrarse y autenticarse. La sesión de claude-design del 2026-05-02 zanjó que cada flow merece su propia ruta con layout diferenciado, sin tabs.

## Acceptance Criteria

- [ ] 4 rutas separadas en `app/(auth)/`:
  - `/auth/sign-up` — formulario de registro.
  - `/auth/sign-in` — formulario de login.
  - `/auth/forgot` — pedir reset de contraseña.
  - `/auth/forgot-sent` — pantalla de "te mandamos el link".
- [ ] Cada ruta es Server Component thin que importa el componente del feature correspondiente.
- [ ] El layout `(auth)/layout.tsx` mantiene el guard de redirect a `/home` si ya hay sesión.
- [ ] El componente `AuthView` con tabs se elimina (deuda explícita de S1 cerrada).
- [ ] Las features `sign-up`, `sign-in`, `forgot-password` ya existen (S1) y no requieren rebuild de su lógica — solo se desacoplan del shell con tabs y se conectan a la nueva ruta.
- [ ] Footer links cross-flow:
  - Sign-up: "¿ya tenés cuenta? Iniciá sesión" → `/auth/sign-in`.
  - Sign-in: "¿sos nuevo? Creá tu cuenta" → `/auth/sign-up`.
  - Sign-in: "¿olvidaste tu contraseña?" → `/auth/forgot`.
- [ ] Tests E2E existentes (`forgot-password.spec.ts`, `sign-in.spec.ts`, `resend-verification.spec.ts`) actualizados a las nuevas rutas.

## Sub-tasks

- [ ] `app/(auth)/sign-up/page.tsx` — wrapper que importa `<SignUpForm>` del feature.
- [ ] `app/(auth)/sign-in/page.tsx` — wrapper que importa `<SignInForm>`.
- [ ] `app/(auth)/forgot/page.tsx` — wrapper para forgot-password feature.
- [ ] `app/(auth)/forgot-sent/page.tsx` — pantalla de confirmación de envío del mail de reset.
- [ ] Borrar `app/(auth)/auth/page.tsx` (la ruta unificada con tabs).
- [ ] Borrar `components/layout/auth-view.tsx` y refs.
- [ ] Update redirects desde `forwardSetCookies` y server actions para que vayan a las rutas nuevas.
- [ ] Actualizar tests E2E con nuevos selectors / paths.

## Notas de implementación

- **Backend no cambia**: los endpoints `/api/identity/sign-up`, `/api/identity/sign-in`, `/api/identity/forgot-password`, `/api/identity/resend-verification` quedan como están. Solo el shell HTML cambia.
- **Cookie session no cambia**: el flow de JWT + refresh token se preserva.
- **Layout diferenciado por ruta**: el mockup muestra que sign-in tiene un panel lateral con "última actividad / stats" que no aparece en sign-up. Cada ruta arma su propio layout.

## Refs

- DoD: [Definition of Done](../definition-of-done.md)
- Mockup: bundle de claude-design del 2026-05-02, sección ② Auth artboards `signup`, `login`, `forgot`, `forgot-ok`.
- ADRs: [ADR-0041](../../decisions/0041-rediseno-ux-post-claude-design.md), [ADR-0019](../../decisions/0019-single-nextjs-app-con-route-groups.md), [ADR-0023](../../decisions/0023-auth-flow-jwt-cookie-layout-guards.md).
