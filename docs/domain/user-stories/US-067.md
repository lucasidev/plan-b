# US-067: Crear cuentas staff

**Status**: Parcial (S7). **Entregado**: `User.RegisterStaff` (factory de dominio, con `email_verified_at` seteado) y el provisioning vía `IdentitySeeder` desde `personas.json`, que es lo que habilitó las personas staff de los tests y del dev stack. **Pendiente**: el endpoint, el comando, `must_change_password` y la UI. Hoy no se puede dar de alta un moderador desde el producto, solo por seed.
**Sprint**: S7
**Epic**: [EPIC-09: Backoffice de cuentas staff](../epics/EPIC-09.md)
**Priority**: Medium
**Effort**: S
**UC**: [UC-067](../use-cases/UC-067.md)
**ADR refs**: ADR-0008

## Como admin, quiero crear cuentas con role moderator/admin/university_staff para incorporar gente al equipo operativo

Como admin, quiero un endpoint que cree Users con role staff, email auto-verified (no hay flow de auto-registro para estos roles, ADR-0008).

## Acceptance Criteria

### Backend

> TODO(doc): el AC completo (endpoint HTTP self-service de backoffice) **no está implementado**.
> Lo que existe es un nivel más bajo: `User.RegisterStaff` (`User.cs`), un factory de dominio que
> crea el User verificado con el rol pedido, invocado únicamente por `IdentitySeeder` a partir de
> `personas.json` (config estática, dev-only: un developer edita el JSON y reinicia el proceso). No
> hay ningún comando/endpoint que un admin logueado pueda invocar desde el backoffice para crear
> una cuenta staff en runtime. No existe el namespace `/api/admin/...` ni ninguna otra ruta para
> esto (commit `feat(identity): provisioning de cuentas staff (US-067)`, 8a1da6a).

- [ ] `POST /api/identity/staff-users` con `{ email, password, role }` (role: `moderator` | `admin` | `university_staff`). El AC original decía `/api/admin/...`, que no existe: `User` vive en Identity ([ADR-0050](../../decisions/0050-backoffice-como-corte-transversal.md)).
- [x] Email se persiste con `email_verified_at = now()` (auto-verified).
  > TODO(doc): comportamiento verificado en `User.RegisterStaff` (`User.cs:105`) y testeado en `UserTests.cs`; solo alcanzable hoy vía el seeder, no vía endpoint.
- [x] No hay flow de auto-registro para estos roles ([ADR-0008](../../decisions/0008-roles-exclusivos-profiles-como-capacidades.md)). El endpoint de `/sign-up` rechaza role staff.
  > TODO(doc): `RegisterUserRequest`/`RegisterUserEndpoint` (`/api/identity/register`, código real) no tiene campo `role` en absoluto: no hay forma de pedir un rol staff vía auto-registro, lo cual cumple la intención del AC aunque no exista un rechazo explícito con ese nombre.
- [x] Password hasheado con BCrypt cost 12 (mismo que registro normal).
- [ ] Si role = `university_staff`, requiere `universityId` para scopear el dashboard.
  > TODO(doc): `User.RegisterStaff` no toma `universityId`; no implementado.
- [x] No puede crear `StudentProfile` ni `TeacherProfile` para users staff (invariante).
  > TODO(doc): verificado para StudentProfile (`User.AddStudentProfile` rechaza si `Role != Member`, `User.cs:548`); no se verificó el mismo chequeo para TeacherProfile puntualmente.
- [ ] Requiere `role = 'admin'`.
  > TODO(doc): no aplica, no hay endpoint que gatear.
- [ ] Force change password en first login: el User creado por admin queda con flag `must_change_password=true`. El próximo sign-in exitoso devuelve un response específico (200 + body `{ mustChangePassword: true }`) que el frontend usa para redirigir a `/change-password` antes del dashboard. La flag se limpia en el cambio.
  > TODO(doc): no existe. No hay columna `must_change_password` en el código (grep sin resultados).

### Frontend

- [ ] UI admin para staff onboarding con copy claro sobre permisos.
  > TODO(doc): no existe. No hay ninguna feature de "crear cuenta staff" en el frontend.

## Sub-tasks

- [ ] Comando `CreateStaffUserCommand`
  > TODO(doc): no existe. Solo el domain method `User.RegisterStaff`, invocado directo por el seeder (sin Command/Handler Wolverine de por medio).
- [ ] Endpoint Carter
- [ ] Authorization admin only
- [ ] UI admin
- [ ] Migration: agregar columna `must_change_password BOOLEAN DEFAULT false` a `identity.users`.
- [ ] Integration tests: admin crea moderator, admin crea university_staff con universityId, member intenta crear staff rechazado, role inválido rechazado
  > TODO(doc): no hay integration tests HTTP de este flujo (no hay endpoint que testear). Sí hay domain unit tests de `User.RegisterStaff` en `UserTests.cs` (`RegisterStaff_creates_verified_active_account_with_the_given_role`, `RegisterStaff_rejects_member_role`, `RegisterStaff_fails_when_password_hash_is_blank`).

## Notas de implementación

- **No auto-registro para staff**: ADR-0008. Los roles `moderator`, `admin`, `university_staff` son administrativos. La incorporación es manual y trazable. Evita escalation de privilege por self-service.
- **Email auto-verified**: el admin que crea es responsable de verificar la identidad fuera de banda. No tiene sentido mandar mail de confirmación a una cuenta operativa.
- **`university_staff` requiere scope**: el dashboard institucional (US-008) solo muestra data de la university scopeada. Crear un staff sin scope sería inseguro (default access-all).

## Refs

- DoD: [Definition of Done](../definition-of-done.md)
- Use Case: [UC-067](../use-cases/UC-067.md)
- ADRs: [ADR-0008](../../decisions/0008-roles-exclusivos-profiles-como-capacidades.md)
