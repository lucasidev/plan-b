# US-002: Ver materia con sus reseñas

**Status**: Done
**Sprint**: S6
**Epic**: [EPIC-01: Catálogo público y exploración](../epics/EPIC-01.md)
**Priority**: High
**Effort**: M
**UC**: [UC-002](../use-cases/UC-002.md)
**ADR refs**: ADR-0009

## Como visitante anónimo, quiero ver una materia con sus reseñas anonimizadas y agregados para evaluarla antes de inscribirme

Como visitante anónimo, quiero la página de una materia con metadata, agregados (rating promedio, histograma) y lista de reseñas published anonimizadas, para poder decidir si me interesa cursarla.

## Acceptance Criteria

### Backend

- [x] `GET /api/subjects/{id}` retorna metadata (`code`, `name`, `year_in_plan`, `weekly_hours`, `total_hours`, `description`) + agregados (rating promedio de `difficulty_rating`, histograma 1..5, conteo total).
- [x] `GET /api/subjects/{id}/reviews` retorna lista paginada de reviews `status = 'published'`, sin identidad del autor.
- [ ] Cada review muestra `difficulty_rating`, `subject_text`, `teacher_text`, nombre del docente reseñado (Teacher en title case), fecha relativa.
- [x] Reviews `under_review` y `removed` se excluyen del listado público (data-model invariante de moderación).
- [x] Sin autenticación requerida.
- [x] Empty state: 'Esta materia todavía no tiene reseñas. Sé el primero cuando termines de cursarla.'
- [x] Si el subject existe pero no tiene reviews published: el endpoint devuelve 200 con array vacío + agregados en cero, NO 404.

### Frontend

- [x] Página `/materias/[id]` (RSC) con metadata + agregados arriba, lista de reseñas debajo.
- [ ] Paginación cursor-based con pageSize=20 default, max 100 (Dapper read).

## Sub-tasks

- [x] Read model de agregados con Dapper (avg + histogram + count)
- [x] Endpoint Carter listing reviews con paginación
- [x] Anonimización en proyección (no en storage). El read model nunca selecciona `enrollment.student_id` ni `user_id`
- [ ] Página frontend con paginación cursor-based (pageSize=20 default, max 100).
- [x] Integration tests: review removed no aparece, review under_review no aparece, autor no se filtra en JSON

## Notas de implementación

- **Anonimato como regla de presentación**: ADR-0009 + invariante transversal del data-model. La identidad del autor existe en DB (necesaria para moderación) pero los DTOs públicos nunca la incluyen. Test de integración explícito que chequea ausencia de `student_id`, `user_id`, `email` en la respuesta.
- **Histograma agregado desde Dapper**: una sola query con `GROUP BY difficulty_rating` evita N+1 y mantiene el endpoint barato. Si el volumen crece a millones, considerar materializar en un read model con event-driven updates desde `ReviewPublished`.
- **Filtrado de status en query, no en aplicación**: el WHERE filtra `status = 'published'` directamente en SQL. Si el filtro fuese en código se podrían leakear reviews `under_review` por error.

## Implementación (S6)

El doc de arriba es pre-US-089 (hablaba solo de promedio de `difficulty_rating` y ruta `/materias/[id]`). Lo entregado, reconciliado con el modelo rico que US-089 hizo persistible:

- **Ruta**: pública `(public)/subjects/[id]` por UUID (el code no es único entre planes). Se eliminó el stub obsoleto `(member)/subjects/[code]` (ComingSoon que apuntaba a esta US; nada lo linkeaba; Next.js no permite `[code]` y `[id]` para el mismo path). Página server-rendered, paginación por links `?page=N`.
- **Endpoints**: `GET /api/academic/subjects/{id}` (metadata, 404 si no existe) + `GET /api/reviews/insights?subjectId={id}` (agregados) + el `GET /api/reviews?subjectId=` existente, extendido con los campos de US-089. Tres llamadas que la página compone.
- **Crowd insights** (una query Dapper con `AVG` + `COUNT FILTER`): promedio de `overall_rating`, histograma 1..5 de overall, promedio de dificultad y de horas/semana, % `would_recommend_course`, conteo. Materia vacía: 200 con count 0 y promedios null.
- **Lección Dapper**: una columna `text[]` rompe el constructor-matching de records posicionales de Dapper (busca `System.Array`); el row del browse se mapea con clase + property-setters.
- **Diferido**: comisiones + nombre del docente por reseña + filtro por docente (bloqueado por Teacher/Commission, US-063); "Aprobación histórica" del mockup (no hay read model de pass-rate); votos útil/no-útil (US-040).

## Refs

- DoD: [Definition of Done](../definition-of-done.md)
- Use Case: [UC-002](../use-cases/UC-002.md)
- ADRs: [ADR-0009](../../decisions/0009-anonimato-como-regla-de-presentacion.md)
- Mockup: `docs/design/reference/components/details.jsx` (ver `SubjectView` para layout de metadata + agregados + lista de reviews)
