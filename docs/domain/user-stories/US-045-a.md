# US-045-a: Mi carrera shell + nav de tabs

**Status**: Done
**Sprint**: S2
**Epic**: [EPIC-03: Historial académico](../epics/EPIC-03.md)
**Priority**: High
**Effort**: S
**Parent US**: [US-045](US-045.md) (Mi carrera consolidada)
**ADR refs**: [ADR-0019](../../decisions/0019-single-nextjs-app-con-route-groups.md), [ADR-0041](../../decisions/0041-rediseño-ux-post-claude-design.md)

## Como member, quiero entrar a `/mi-carrera` y ver una sola pantalla con tabs claros para no saltar entre 4 entradas distintas del sidebar

Primer slice del rebuild de "Mi carrera". Crea la ruta `/mi-carrera`, el layout con nav de 5 tabs, el routing por query param + URL, y los stubs vacíos de cada tab. Los tabs reales (Plan, Correlativas, Materias + Docentes, Historial) aterrizan en `-b`, `-c`, `-d`, `-e`.

También consolida el sidebar: borra las 4 entradas viejas (`/plan`, `/subjects`, `/professors`, `/history`) y deja solo "Mi carrera".

## Acceptance Criteria

- [x] Ruta `app/(member)/mi-carrera/page.tsx` (server component) con tabs nav + render del tab activo según `?tab=...`. **Nombre real**: `app/(member)/my-career/page.tsx` (la ruta se renombró a inglés en el refactor de PR #137, S5; ver `frontend/CLAUDE.md`). Mismo comportamiento.
- [x] **5 tabs** visibles en la nav: Plan / Correlativas / Materias / Docentes / Historial. Default activo: `plan` (si no hay query param o el valor es inválido). Confirmado en `features/my-career/lib/tabs.ts` (`MY_CAREER_TABS`).
- [x] Cambiar de tab actualiza la URL (`router.replace` o `<Link>` para que sea sharable). Back / forward del browser navega entre tabs sin perder la sesión.
- [x] Cada tab renderea por ahora un **stub `ComingSoon` específico** con copy "Próximamente: <nombre del tab>". El stub se reemplaza por el componente real cuando aterrice el slice correspondiente (`-b` Plan, `-c` Correlativas, `-d` Materias + Docentes, `-e` Historial). Los 5 slices hermanos ya cerraron (S3): los stubs ya no existen, reemplazados por `plan-grid.tsx`, `prerequisites-graph.tsx`, `subject-list.tsx` / `teacher-list.tsx`, `history-tab.tsx`.
- [x] **Sidebar update**: borrar entries `/plan`, `/subjects`, `/professors`, `/history` del componente `Sidebar`. Agregar entry única "Mi carrera" → `/mi-carrera`. Confirmado en `lib/member-shell.ts`: una sola entry "Mi carrera" → `/my-career`.
- [x] **Borrar stubs viejos** `app/(member)/{plan,subjects,professors,history}/page.tsx` con sus layouts si existen. Si tienen tests, borrarlos también. Se tomó la alternativa de redirect (ver AC de abajo) en vez de borrado simple para 3 de las 4 rutas.
- [x] **Redirects de las rutas viejas** (opcional pero deseable): `/subjects` → `?tab=catalog` y `/professors` → `?tab=teachers` y `/history` → `?tab=transcript` están implementados y citan "(US-045-a)" en su comentario. Parcial: **`/plan` no redirige a `/my-career?tab=plan`**, esa ruta fue reclamada después por US-046 (Planificar) y hoy sirve esa pantalla distinta; no es un gap de esta US, es un cambio de dueño del path posterior.
- [ ] **Drawers** (`/mi-carrera/materia/[code]` y `/docente/[id]`): out of scope acá. Llegan con `-d`. (Declaración de scope, no un entregable de este slice.)

## Out of scope

- Contenido real de los tabs: 100% stubs `ComingSoon` en este slice.
- Drawers de detalle (materia / docente): aterrizan con `-d`.
- Loading / error states avanzados de los tabs reales: cada slice los define.
- Cambio del look del sidebar (rebuild visual): solo se cambian las entries, no el estilo.
- Empty states avanzados de cada tab: definidos por cada slice.

## Edge cases

| Caso | Comportamiento esperado |
|---|---|
| User entra a `/mi-carrera` sin `?tab` | Default tab `plan` activo. La URL no se reescribe a `?tab=plan` (o sí, según convención: MVP no reescribe, evita hops innecesarios). |
| User entra a `/mi-carrera?tab=invalido` | Fallback al default `plan`, mensaje en consola con warning. No 404. |
| User sin StudentProfile entra a `/mi-carrera` | Guard del `(member)/layout.tsx` redirige a `/onboarding/welcome` (cubierto en US-037-f, no es responsabilidad de este slice). |
| User entra a `/plan` (legacy) | Redirige a `/mi-carrera?tab=plan` con `redirect()` server-side. |
| Click en tab actual | No hace nada (no re-render, no nuevo entry en history). |
| Browser back desde tab `historial` a `plan` | URL cambia, contenido cambia, no recarga la app. |

## Test scenarios

### Críticos (Given-When-Then)

1. **Given** Lucía con StudentProfile, **when** entra a `/mi-carrera`, **then** ve los 5 tabs con `Plan` activo + stub "Próximamente: Plan".
2. **Given** Lucía en `/mi-carrera?tab=plan`, **when** clickea el tab "Correlativas", **then** la URL cambia a `?tab=correlativas` y el stub correspondiente aparece.
3. **Given** Lucía en `/mi-carrera?tab=correlativas`, **when** hace browser back, **then** vuelve a `?tab=plan` y el stub Plan aparece.
4. **Given** Lucía entra a `/plan` (legacy), **when** la página resuelve, **then** se la redirige a `/mi-carrera?tab=plan`.
5. **Given** Lucía sin profile, **when** entra a `/mi-carrera`, **then** el guard la lleva a `/onboarding/welcome` (verificación cross-cutting, no test propio).

### Cobertura por capa

- **Component / vitest + RTL**: `mi-carrera-tabs.test.tsx` que renderea con mock `useSearchParams` y verifica que el tab activo refleje el query param.
- **E2E Playwright**: spec liviano `mi-carrera-shell.spec.ts` que navega tabs + browser back. Confirma que las URLs cambian. No asserta contenido (los stubs no tienen contenido testeable).

## Sub-tasks

- [x] `app/(member)/mi-carrera/page.tsx`: server component con `searchParams` + render condicional del tab. Ruta real `app/(member)/my-career/page.tsx` (renombrada a inglés, PR #137).
- [x] `features/mi-carrera/components/tabs-nav.tsx`: tabs nav con `Link` de Next.js. Activo según `searchParams.tab`. Ruta real `features/my-career/components/tabs-nav.tsx`.
- [x] `features/mi-carrera/components/{plan-stub,correlativas-stub,catalogo-stub,docentes-stub,historial-stub}.tsx`: 5 stubs `ComingSoon` con copy específico. Ya reemplazados por los componentes reales de los slices `-b/-c/-d/-e` (S3).
- [x] Helper `parseTab(value)` en `features/mi-carrera/lib/tabs.ts`: valida valor, fallback a `plan`. Tests aparte. Confirmado `features/my-career/lib/tabs.ts` + `tabs.test.ts`.
- [x] Update `components/layout/Sidebar.tsx` (o donde viva): remover 4 entries, agregar "Mi carrera".
- [x] Borrar `app/(member)/plan/page.tsx`, `subjects/page.tsx`, `professors/page.tsx`, `history/page.tsx` + sus tests. Se tomó la vía del sub-task de abajo (redirects) para `subjects/professors/history`; `plan/page.tsx` hoy es la ruta real de US-046 (Planificar), no un stub legacy de Mi carrera.
- [x] (Opcional) Crear `app/(member)/{plan,subjects,professors,history}/page.tsx` mínimos que solo hacen `redirect('/mi-carrera?tab=...')` para legacy. Borrar definitivamente cuando se confirme que no hay bookmarks externos. Hecho para `subjects` → `?tab=catalog`, `professors` → `?tab=teachers`, `history` → `?tab=transcript`. `plan` no aplica (ver nota AC).
- [ ] Spec E2E `frontend/e2e/dashboard/mi-carrera-shell.spec.ts`: navega tabs + browser back + redirect legacy. No existe `frontend/e2e/dashboard/` en el repo: sin spec E2E dedicado al shell de Mi carrera.

## Notas de implementación

- **Tab activo via searchParams (server)**: el server component lee `searchParams.tab`, valida con `parseTab`, y pasa el activo a `TabsNav` como prop. `TabsNav` es client component porque genera links + maneja highlight.
- **Tabs nav vs route group nested**: optamos por single page + query param (no nested layouts) porque los tabs son del mismo dominio, sharable, y no cambia el shell. Convención registrable en convenciones de routing si es necesaria.
- **Stubs específicos vs un único `ComingSoon` parametrizado**: optamos por 5 archivos individuales para que cuando aterricen los slices reales, solo se cambia el import en `page.tsx` + se borra el stub. Limpieza mecánica.
- **Sidebar update**: ojo con tests del sidebar (snapshot / role-based). Si rompen, actualizar.
- **Redirects legacy**: con `redirect('/mi-carrera?tab=plan')` server-side. Una línea de código, vale la pena.

## Dependencies

- **Depende de**: [US-037-f](US-037-f.md) (guard de `(member)`, **Done**) y [US-042-f](US-042-f.md) (AppShell con sidebar, **Done**).
- **Bloquea a**: [US-045-b](US-045-b.md), [US-045-c](US-045-c.md), [US-045-d](US-045-d.md), [US-045-e](US-045-e.md). Todos necesitan que el shell + tabs nav exista.
- **Relacionada con**: ninguna directa.

## Refs

- DoD: [Definition of Done](../definition-of-done.md)
- Parent US: [US-045](US-045.md)
- Slices hermanos: [US-045-b](US-045-b.md), [US-045-c](US-045-c.md), [US-045-d](US-045-d.md), [US-045-e](US-045-e.md)
- Mockup: ![Plan de estudios](../../design/reference/screenshots/plan-v2-carrera-p.png). El nav de tabs (Plan, Correlativas, Materias, Docentes, Historial) es consistente entre las 5 vistas. Fuente JSX en `canvas-mocks/v2-screens-2.jsx::V2MiCarrera`.
- ADRs: [ADR-0019](../../decisions/0019-single-nextjs-app-con-route-groups.md), [ADR-0041](../../decisions/0041-rediseño-ux-post-claude-design.md).
