# US-065: Gestionar Commission + CommissionTeacher

**Status**: Parcial (S7). **Entregado**: el aggregate `Commission` + `CommissionTeacher`, el seed que los carga y los GET públicos de lectura, que es lo que necesitaba US-003 para sacar el `PLACEHOLDER_TEACHER_ID`. **Pendiente**: el CRUD de backoffice entero (crear/editar comisiones y asignar docentes desde el admin) y su UI. Hoy las comisiones solo entran por seed.
**Sprint**: S7
**Epic**: [EPIC-08: Backoffice de catálogo](../epics/EPIC-08.md)
**Priority**: Medium
**Effort**: M
**UC**: [UC-065](../use-cases/UC-065.md)
**ADR refs**: ninguno.

## Como admin, quiero CRUD de Commissions y asignación de teachers para que los alumnos puedan referenciar comisiones reales

Como admin, quiero CRUD de Commission (Subject + AcademicTerm + name + modality + capacity) y asignar teachers con role.

## Acceptance Criteria

### Backend

> TODO(doc): el AC completo de CRUD admin (create/update de Commission, asignar/desasignar
> teacher) **no está implementado**. El commit que fundó Commission (`feat(academic): comisiones +
> asignación de docentes (US-065)`) lo dice explícito en su propio mensaje: "Diferido: CRUD admin
> de comisiones + UI (admin shell), validación cross-aggregate de coherencia (va con el handler
> admin Create)". Lo que sí existe:
> - El aggregate `Commission` (domain) completo, con métodos `Create`/`Update`/`AssignTeacher`/
>   `UnassignTeacher` listos para un CRUD futuro, pero sin comando/endpoint que los invoque.
> - Seed data (10 comisiones demo UNSTA con docentes asignados).
> - Dos reads públicos de solo lectura: `GET /api/academic/subjects/{subjectId}/commissions`
>   (`ListCommissionsEndpoint.cs:21`) y `GET /api/academic/commissions/{commissionId}/teachers`
>   (`GetCommissionTeachersEndpoint.cs:21`).
> No hay ningún endpoint `/api/admin/...` ni `/api/academic/...` que cree, actualice o asigne
> teachers a una comisión. Los checkboxes de abajo quedan sin marcar en consecuencia.

- [ ] CRUD Commission bajo `/api/academic/subjects/{subjectId}/commissions` (módulo dueño de `Commission`; el AC original decía `/api/admin/...`, que no existe, [ADR-0050](../../decisions/0050-backoffice-como-corte-transversal.md)):
  - Create con `{ termId, name, modality, capacity?, notes? }`.
  - `UNIQUE(subject_id, term_id, name)`.
  - Validación: `subject.term_kind = term.kind` (coherencia de cadencia).
  - Validación: misma university (`subject.career_plan.career.university = term.university`).
- [ ] CRUD CommissionTeacher bajo `/api/academic/commissions/{commissionId}/teachers`:
  - Add teacher con `{ teacherId, role }` (role: `titular` | `adjunto` | `jtp` | `ayudante` | `invitado`).
  - PRIMARY KEY `(commission_id, teacher_id)`.
  - Validación: `teacher.university = subject.career_plan.career.university`.
- [ ] Requiere `role = 'admin'`.
- [ ] Cambio de modality después de tener inscriptos: rechazar con 409 `academic.commission.has_enrollments`. El admin debe crear una commission nueva con la modality correcta y migrar inscriptos manualmente (post-MVP automatizable).
  > TODO(doc): no hay ningún chequeo de inscriptos en `Commission.Update` (código real); esta validación no está implementada ni a nivel de dominio.

### Frontend

- [ ] UI grid de comisiones por term + asignación de teachers.
  > TODO(doc): no existe. No hay ninguna carpeta `admin/commissions` ni `admin/subjects` en el frontend.

## Sub-tasks

- [x] Aggregate Commission + child entity CommissionTeacher
- [ ] Domain validators (coherencia universitaria total, ADR-0001)
  > TODO(doc): el propio doc comment del aggregate dice que esa validación "la valida el app service que cree comisiones, no el aggregate"; ese app service (el CRUD admin) no existe todavía.
- [ ] Endpoints Carter
  > TODO(doc): solo existen los dos GET públicos de solo lectura (ver nota arriba), no el CRUD admin.
- [ ] UI admin
- [ ] Integration tests: UNIQUE name por subject+term, term_kind mismatch rechazado, teacher de otra university rechazado
  > TODO(doc): estos escenarios validarían el CRUD admin, que no existe. Sí hay 16 domain unit tests de las invariantes internas del aggregate (`CommissionTests.cs`) y 4 integration tests de los reads públicos, pero no del CRUD admin descrito acá.

## Notas de implementación

- **Coherencia universitaria total**: invariante data-model. `Career.university = Subject.career_plan.career.university = AcademicTerm.university = Teacher.university`. El handler valida cross-table antes de persistir.
- **CommissionTeacher como child entity**: vive en el aggregate Commission. PRIMARY KEY compuesto `(commission_id, teacher_id)` permite múltiples teachers por commission con roles distintos (titular + adjunto + JTP).
- **Capacity opcional**: algunas comisiones no tienen cap formal. Se modela como nullable.

## Refs

- DoD: [Definition of Done](../definition-of-done.md)
- Use Case: [UC-065](../use-cases/UC-065.md)
- Mockup admin canvas (sección ②):
  - ![Comisiones · oferta 2026·1c](../../design/reference/screenshots/admin-datos-adm-com-list.png)
  - Fuente JSX en `canvas-mocks/admin-screens-2.jsx::AdmComisiones`. Agregar AC visual con selector de cuatri (cruza US-064 AcademicTerm), campos extra (aula, cupo), flags "sin docente" / "sin horario" + CTA "Importar oferta".
- ADRs: [ADR-0041](../../decisions/0041-rediseño-ux-post-claude-design.md)
